export const config = {
   mode: "free",
   docMode: "both",
};

export const scrollBar = {
   scrollPosition: 0,
   isDragging: false,
   startY: 0,
};

export const Scale = {
   scale: 1,
   scalingFactor: 1.1,
};
