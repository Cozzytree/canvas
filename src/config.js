export const config = {
  mode: "free",
  docMode: "both",
};

export const scrollBar = {
  scrollPositionY: 0,
  scrollPositionX: 0,
  isDraggingY: false,
  isDraggingX: false,
  startY: 0,
  startX: 0,
};

export const Scale = {
  scale: 1,
  scalingFactor: 1.1,
};
